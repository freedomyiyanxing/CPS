const dropStyle = theme => ({
  dropDownContainer: {
    position: 'absolute',
    top: 30,
    left: 0,
    width: '100%',
  },
  dropDownWrapper: {
    background: theme.palette.primary.A400,
    height: 200,
    position: 'relative',
    top: 10,
  },
});

export default dropStyle;
