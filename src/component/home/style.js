export const style1 = theme => ({
  root: {
    background: theme.palette.primary[500],
  },
  wrapper: {
    marginBottom: 10,
  },
});
export const style12 = theme => ({
  background: theme.palette.primary[500],
});
