const not404Style = theme => ({
  root: {
    background: theme.palette.primary[50],
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
    flex: 1,
  },
});

export default not404Style;
